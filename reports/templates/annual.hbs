{{!-- ═══════════════════════════════════════════════════════════════
  annual.hbs — Annual executive report

  Pages:
    1. Executive snapshot: header + KPI strip + narrative
    2. Evidence: Recommendations + Observations (rendering contract)

  Reads ctx produced by reports/content/index.js::buildReportContext
  with variant='annual'. Annual uses the same 3-rec cap and shared
  evidence partial; the content layer drives content depth through its
  trigger catalog (T11 and others gated on variant='annual').
═══════════════════════════════════════════════════════════════ --}}

{{!-- ── Page 1 — annual snapshot ──────────────────── --}}
<section class="page">
  <header class="page-chrome">
    <span class="brand-mark">d'strict · GTO</span>
    <span class="meta">
      <span>{{labels.title}}</span>
      <span>·</span>
      <span>{{period}}</span>
      {{#if scope}}<span>·</span><span>{{scope}}</span>{{/if}}
    </span>
  </header>

  <article class="section" style="margin-top:0">
    <header>
      <span class="num">01</span>
      <h2>{{labels.executiveSummary}}</h2>
      <span class="eyebrow">{{#if (eq lang "ko")}}연간 스냅샷{{else}}Annual snapshot{{/if}}</span>
    </header>

    {{> narrative}}
    {{> kpi-strip kpiColumns=(default kpiColumns "5")}}
  </article>

  <footer class="page-footer">
    <span>{{generated}}</span>
    <span>{{labels.page}} 1</span>
  </footer>
</section>

{{!-- ── Page 2 — evidence (recs + observations) ─────── --}}
<section class="page">
  <header class="page-chrome">
    <span class="brand-mark">d'strict · GTO</span>
    <span class="meta">
      <span>{{labels.title}}</span>
      <span>·</span>
      <span>{{period}}</span>
    </span>
  </header>

  <article class="section" style="margin-top:0">
    <header>
      <span class="num">02</span>
      <h2>{{#if recommendations.length}}{{labels.recommendations}}{{else}}{{labels.observations}}{{/if}}</h2>
      <span class="eyebrow">{{labels.executiveSummary}}</span>
    </header>
    {{> evidence}}
  </article>

  <footer class="page-footer">
    <span>{{generated}}</span>
    <span>{{labels.page}} 2</span>
  </footer>
</section>
